# AI 编程规范

## 一、定位

本规范用于约束 AI 编程助手在项目开发、修改、调试、部署和交付过程中的行为，适用于前端、后端、脚本、数据处理、模型接入、部署运维等各类技术项目。

核心目标：

1. 减少误改和漏改；
2. 避免过度设计；
3. 保持代码、配置、文档同步；
4. 提高问题排查和交付复用效率；
5. 让 AI 编程过程可理解、可验证、可回溯。

---

## 二、基本原则

### 1. 先理解，再动手

AI 在写代码或修改文件前，应先理解任务目标、项目上下文和影响范围。

必须做到：

* 不在需求不清时直接实现；
* 不隐藏不确定性；
* 不擅自替用户决定关键技术路线；
* 发现多种解释时，应说明差异；
* 发现用户方向可能偏离目标时，应及时提醒；
* 发现更简单、更稳妥方案时，应主动提出。

对于明显简单、低风险的任务，可以直接执行，但仍应保持修改范围可控。

---

### 2. 最小实现，避免过度设计

AI 应优先选择能解决当前问题的最小方案。

必须避免：

* 未经要求增加功能；
* 未经要求增加配置项；
* 为单次需求设计复杂抽象；
* 为未来可能性提前设计架构；
* 引入不必要依赖；
* 将简单脚本改成复杂框架；
* 将局部修改扩大为系统重构。

判断标准：

> 每一处新增代码，都应能直接对应当前需求。

---

### 3. 精准修改，不做无关重构

修改已有项目时，应只改必须改的地方。

必须遵守：

* 不顺手优化无关代码；
* 不随意调整代码格式；
* 不改动与本任务无关的命名、结构、注释；
* 不删除原本存在但与当前任务无关的代码；
* 不改变用户未要求改变的运行方式；
* 尽量匹配项目原有风格。

如果发现无关问题，可以在回复中提示，但不要擅自处理。

---

### 4. 目标导向，结果可验证

AI 不应只说“已完成”，而应说明如何验证。

常见任务应对应明确验证方式：

| 任务类型   | 验证方式                   |
| ------ | ---------------------- |
| 修复 bug | 说明原问题、修改点、复现/验证方式      |
| 新增接口   | 说明请求方式、参数、返回结果         |
| 前端修改   | 说明页面位置、交互变化、视觉变化       |
| 脚本修改   | 说明执行命令、输入输出、异常处理       |
| 部署修改   | 说明启动命令、端口、日志检查方式       |
| 配置修改   | 说明配置路径、字段含义、生效方式       |
| 模型接入   | 说明 curl 验证、项目内验证、常见失败点 |

---

## 三、任务执行流程

### 1. 任务开始前

AI 应先完成以下判断：

```text
本次任务目标是什么？
涉及哪些文件或模块？
是否需要查阅已有文档或 Skills？
是否存在更简单方案？
是否会影响已有功能？
是否需要同步更新文档？
```

如果任务复杂，应先给出简短计划。

示例：

```text
计划：
1. 定位现有实现位置；
2. 做最小范围修改；
3. 更新相关说明文档；
4. 给出验证方式。
```

---

### 2. 修改过程中

AI 应遵守以下要求：

* 优先阅读现有代码和文档；
* 先定位，再修改；
* 不凭空猜测路径、接口、字段；
* 不破坏现有配置结构；
* 不把临时测试代码留在正式文件中；
* 不把密钥、Cookie、Token 写死进代码；
* 不把本地绝对路径写入通用项目代码；
* 不把调试日志长期保留为生产输出；
* 不引入用户没有要求的新技术栈。

---

### 3. 修改完成后

AI 应在最终说明中包含：

```text
完成内容：
涉及文件：
验证方式：
文档同步情况：
注意事项：
```

如果没有更新文档，应说明原因，例如：

```text
本次只修改局部样式，未涉及架构、接口、目录、部署方式变化，因此未更新文档。
```

---

## 四、项目文档规范

### 1. 根目录主文档

每个项目根目录应保留一个主说明文件，例如：

```text
README.md
项目说明.md
AI编程规范.md
```

主文档应说明：

* 项目定位；
* 技术栈；
* 目录结构；
* 启动方式；
* 构建方式；
* 部署方式；
* 核心配置；
* 常见问题；
* 可复用 Skills 路径。

当项目发生以下变化时，必须同步更新主文档：

* 架构变化；
* 目录变化；
* 启动方式变化；
* 部署方式变化；
* 配置方式变化；
* 重要功能变化；
* 重要依赖变化。

---

### 2. 目录级说明文档

重要目录下建议保留一个简洁说明文件：

```text
README.md
```

目录说明文件建议包含：

```text
一旦我所属的文件夹有所变化，请更新我。

本目录地位：
本目录功能：
关键文件清单：
```

关键文件清单建议采用：

| 文件/目录 | 地位   | 功能     |
| ----- | ---- | ------ |
| xxx   | 核心文件 | 负责 xxx |
| xxx   | 配置文件 | 控制 xxx |
| xxx   | 辅助脚本 | 用于 xxx |

目录内文件发生明显变化时，应同步更新该目录说明。

---

### 3. 代码文件头部注释

核心代码文件建议在开头保留三行极简注释：

```text
input：依赖外部什么输入、配置、接口或数据
output：对外提供什么能力、接口或结果
pos：在系统局部架构中的位置
```

并补充：

```text
一旦我被更新，务必同步更新我的开头注释以及所属文件夹的 README.md。
```

适用对象：

* 后端核心服务文件；
* 前端核心页面或组件；
* 关键脚本；
* 关键配置生成器；
* 模型调用封装；
* 数据处理流程；
* 部署入口脚本。

不建议对所有小文件机械添加，避免文档噪音。

---

## 五、代码与配置要求

### 1. 安全要求

AI 编程时不得：

* 明文写入真实密钥；
* 明文写入真实 Cookie；
* 明文写入真实 Token；
* 将敏感地址、账号、密码硬编码；
* 将临时调试凭据提交为默认配置；
* 把生产数据样本写入测试文件。

推荐方式：

```text
使用 .env
使用 .env.example
使用占位符
使用配置文件模板
在文档中说明填写方式
```

---

### 2. 配置要求

配置文件应做到：

* 字段含义清楚；
* 默认值安全；
* 示例值可替换；
* 路径尽量相对化；
* 环境差异通过 `.env` 或配置文件处理；
* 不把外网、内网、测试、生产逻辑混在代码里。

---

### 3. 日志要求

日志应服务于排查问题，不应制造噪音。

建议包含：

* 关键流程开始和结束；
* 关键配置是否加载成功；
* 外部接口调用是否成功；
* 失败原因和异常摘要；
* 必要的耗时信息。

避免输出：

* 密钥；
* Token；
* Cookie；
* 大段原始数据；
* 大段模型返回；
* 用户隐私信息。

---

### 4. 错误处理要求

错误处理应直接帮助定位问题。

错误信息应尽量说明：

```text
哪里失败；
为什么可能失败；
下一步如何排查。
```

不要只返回：

```text
失败
error
unknown
```

---

## 六、测试与验证要求

### 1. 优先使用项目已有验证方式

AI 不应随意引入新的测试框架。应优先查找项目已有方式，例如：

```text
npm run test
npm run build
pytest
curl
docker compose ps
docker logs
```

---

### 2. 没有测试时给出手工验证步骤

如果项目没有自动化测试，应给出最小手工验证步骤：

```text
1. 启动服务；
2. 打开页面；
3. 执行某个操作；
4. 检查返回结果；
5. 查看日志是否报错。
```

---

### 3. 部署类任务必须说明回滚方式

涉及部署、配置、数据迁移时，应说明：

* 如何备份；
* 如何恢复；
* 哪些目录是持久化数据；
* 哪些文件改错会导致服务无法启动。

---

## 七、文档同步要求

AI 完成修改后，必须说明文档同步情况。

标准格式：

```text
文档同步情况：
- 已更新：xxx/README.md，补充了 xxx。
- 已更新：项目说明.md，补充了 xxx。
```

或：

```text
文档同步情况：
- 未更新。原因：本次仅修改局部实现，不涉及架构、接口、配置、部署或目录变化。
```

如果本次修改涉及以下内容，原则上必须更新文档：

* 新增目录；
* 删除目录；
* 新增核心模块；
* 修改启动命令；
* 修改部署流程；
* 修改配置文件；
* 修改接口路径；
* 修改模型接入方式；
* 修改数据处理流程；
* 修改主要页面结构；
* 修改运行依赖。

---

## 八、Skills 使用规范

项目中可沉淀可复用 Skills。AI 遇到相关任务时，应优先查阅对应文件，不要重新摸索。

当前已整理 Skills：

| Skill 名称       | 适用场景                                    | 文件路径                              |
| -------------- | --------------------------------------- | --------------------------------- |
| 内网大模型接入 Skill  | 内网模型网关、OpenAI 兼容接口、模型测试、模型配置            | `D:\AAA\Skills自用\内网大模型接入复用手册.md`  |
| 麒麟内网离线部署 Skill | 麒麟 Linux、ARM64/aarch64、Docker 离线部署、内网交付 | `D:\AAA\Skills自用\麒麟内网离线部署复用手册.md` |
| 前端视觉风格 Skill   | 前端页面、后台系统、大屏、质检系统、税务 AI 系统视觉规范          | `D:\AAA\Skills自用\前端视觉风格tax.md`    |

使用要求：

```text
如果任务命中某个 Skill 的适用场景，应先查阅对应 Skill 文件，再执行。
如果当前信息不足，应主动说明需要查阅哪个 Skill。
如果 Skill 与当前项目实际情况冲突，应以当前项目真实代码和配置为准。
```

---

## 九、推荐给 AI 的固定提示词

```text
请遵守《AI编程规范.md》。

要求：
1. 先理解任务目标和影响范围，再修改。
2. 只做与当前需求直接相关的最小改动。
3. 不做无关重构，不擅自新增功能。
4. 不硬编码密钥、Cookie、Token、个人路径。
5. 修改核心代码后，同步检查是否需要更新 README 或相关文档。
6. 如果涉及内网模型、麒麟离线部署、前端视觉风格，请先查阅对应 Skills 文件。
7. 完成后请说明：完成内容、涉及文件、验证方式、文档同步情况、注意事项。
```

---

## 十、AI 回复格式建议

### 1. 代码修改类任务

```text
完成内容：
- xxx

涉及文件：
- xxx

验证方式：
- xxx

文档同步情况：
- xxx

注意事项：
- xxx
```

### 2. 方案咨询类任务

```text
建议方案：
- xxx

实施步骤：
- xxx

风险点：
- xxx

需要查阅的 Skills：
- xxx

下一步建议：
- xxx
```
